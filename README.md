This is a experimental accelerator for packaging.version.

Run benchmarks with something like this:

```
uv run pytest --benchmark-only tests/test_benchmark_cmp_version.py --benchmark-columns 'mean' --benchmark-group-by=param
```
